package com.milano.quotation.imports;import org.springframework.data.jpa.repository.JpaRepository;import java.util.*;public interface ImportPartRepository extends JpaRepository<ImportPart,UUID>{List<ImportPart>findByJobIdOrderByPartNumber(UUID jobId);Optional<ImportPart>findByJobIdAndPartNumber(UUID jobId,int partNumber);long countByJobIdAndStatus(UUID jobId,String status);}
