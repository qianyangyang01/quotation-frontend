# 渠道税费国家搜索体验

- 基线：生产 -14 对应提交 `d585f33f927d18c7990f9bdde46ec2dbf65b77f5`。
- 工作区：`tax-country-search-20260916`；分支：`codex/tax-country-search-20260916`。
- 原因：上方搜索仅过滤 selected 国家，下方原生 select 展开全部未添加国家，二者搜索互不关联。
- 修改：一个输入框同时搜索已添加和可添加国家，支持中文、英文、两位代码；代码精确匹配，避免 RO 命中 Croatia / European Union。可添加结果直接添加并自动定位，清空搜索；已添加国家及中英文/代码别名不重复添加。
- 空输入默认只显示已添加国家；点击添加后按10条递增浏览，无长原生下拉框。提供清空搜索、无结果反馈、保存中禁用和欧盟独立设置风险提示。
- 数据边界：仅沿用原先将国家 selected 设为 true 的草稿操作，不自动发布、不复制欧盟规则、不改变金额/计费优先级/附加费，不访问或修改生产数据。
- 验证：最终完整前端测试758通过、2跳过；TypeScript检查、Vite生产构建、涉及文件ESLint、git diff --check通过。
- 浏览器：本地隔离模拟页验证中文、RO、Romania查询，直接添加后切换罗马尼亚、清空输入及显示规则提示；RO最终只返回一个可添加结果，浏览器错误日志为空。并非生产页面验收。
- 发布授权：用户已要求提交并部署；目标 `quotation-2026.09.16-15`，发布结果以版本包验收记录及生产 manifest 为准。用户桌面主目录原有修改未触碰。
