# 报价记录列表样式调整

基线：0c6d225（quotation-2026.09.23-12）。
工作区：C:/Users/25490/Documents/ProjectWorkspaces/quotation/worktrees/record-list-style-20260923。

按用户确认的参考图调整列表展示：SKU 18px、客户名和报价规模 16px、说明文字 13px；客户级别移至客户名下方，创建时间合并到商品信息；状态和再次发起并排；导出按钮与日期筛选同行；扩大宽屏内容区域。记录之间使用 2px 灰蓝色横向分隔线，增强区分。

原来的 .empty 样式误命中没有图片的商品组件，使 48px 占位图附加四周 60px 内边距，导致占位区域达到 168px、整行约 197px。空列表样式现只作用于列表直接子节点，普通记录在本地浏览器中约 129px 高。长商品名、长客户名和审核说明允许换行。

仅修改 QuotationRecordsView.vue 的模板结构和样式，以及现有客户级别测试的位置断言。统一换行符后脚本区逐字一致，285 处模板业务绑定和表达式核对一致；计算、价格、保存、审核权限、查询和分页逻辑不变。详情面板样式保持原样。

验证：原有 4 个列表相关测试文件共 16 项通过；TypeScript、Vite 构建、涉及文件 ESLint 通过。使用真实组件与本地模拟 API 在 1440、1920、390 像素宽度验收；桌面布局不溢出，手机仅表格内部横向滚动。检查新客户、普通客户、S 级、长名称、拒绝审核、已成交等状态。

预览入口：http://127.0.0.1:5186/tmp/record-style-preview.html。该入口位于忽略的 tmp 目录，只包含模拟数据并阻止写操作，不连接生产 API。用户已授权提交并部署；发布结果记录于对应版本发布包。
