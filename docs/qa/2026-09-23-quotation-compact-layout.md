# 报价页紧凑布局本地验收

## 范围

- 基线：68d6551。独立分支 codex/quotation-compact-layout-20260923。
- 新增 src/styles/quotationCompact.css；QuotationSystemView.vue 仅在末尾引入样式。
- 不修改组件脚本、props、事件、价格计算、数据读取、草稿、保存、模板逻辑或后端。
- 主目录和其他任务工作区未写入本次改动；模板明细任务已告知其另行发布 8a66c83，后续整合发布必须基于当时最新生产版本，不能直接用本分支旧基线覆盖。

## 布局

- 大于 1100px：六项报价条件同排；佣金阈值灰底，继续可编辑；成本字段和摘要紧凑排列。
- 大于等于 1280px：商品信息与成本并排；国家选择位于渠道表左侧；报价摘要与客户报价单编辑区并排。
- 报价方式、常用国家区域、选中国家/渠道和加入报价单使用绿色；灰色区域用于前置条件和固定资料；保留错误/警示样式。
- 手机使用原有单列布局。表格沿用横向滚动；不缩小生成报价图片。

## 检查结果

- 49 项既有测试通过：QuotationInteraction、QuotationCommonMatrix.search、CostWeightPanel.precision、QuotationSystemView.flow。
- vue-tsc --noEmit -p tsconfig.app.json --incremental false 通过。
- QuotationSystemView.vue ESLint、git diff --check、最终 Vite build 通过。
- 浏览器使用真实 Vue 组件和本地模拟数据，未登录或操作生产。浏览器 error 日志为空。
- 客户下拉未被卡片裁剪；查询按钮可触发事件；灰底佣金阈值可输入并有焦点标识；手动重量和计抛内容可显示；国家切换、渠道搜索及清空搜索后的选中状态正常；指定模式可以切换。
- 390px 窄屏无整页横向溢出，条件单列。1366px 和 1920px 的六项条件在同一行，无整页横向溢出。

## 高度对照和限制

固定场景：单品、28 个常用国家、15 条候选渠道、每页 5 条、加入 1 条报价、未计抛。测试页含 48px 模拟工具条；真实应用导航和提示可能增加高度。

| 浏览器视口 | 原版整页高度 | 新版整页高度 |
| --- | ---: | ---: |
| 1920 × 1080 | 4297px | 2161px |
| 1366 × 768 | 未测 | 2178px |

1080 高度下约两屏，768 高度下仍接近三屏。更多报价行、组合商品、计抛、校验提示或大字号会增加高度；不能声称所有电脑/所有状态严格两屏。

验收页面和截图在本工作区 .codex-tmp/compact-preview/，属于本地辅助证据，不随代码提交。desktop-before.png 与 desktop-after.png 是同一组数据的整页截图。

当前仅本地完成，没有 GitHub 推送、生产部署或生产登录验收。
