# 未提交改动审查

核对基线为生产 `quotation-2026.09.24-09`，提交 `394dffa24dc2a33cc6b7705966e4b214093906cb`。主目录 HEAD 是较早的 `6ccfcfe`，不能直接将其工作区整体作为发布候选。

## 审查方式

盘点已登记工作区的 Git 状态，排除临时输出和构建产物。对源码、测试和文档先比对当前生产及历史提交中的文件内容，再对未匹配文件进行基于原 HEAD 的三方差异检查。三方差异只写入私有审查目录，没有修改原工作区。

主目录盘点时有 131 个相关文件：57 个与当前生产完全一致、34 个与已有历史提交一致、40 个需要进一步核对。这不是 131 个尚未上线的功能。文件状态可能随着其他进行中的任务变化；原始清单保存在私有输出目录。

## 本次选择提交

保留并验证之前遗漏的五个测试文件变更：

- 报价模板在隐藏后更新、切换模式、恢复草稿时保持当前价格；加拿大同渠道不同分区分别保留价格。
- 财务关税与附加费分别按整单收费，不随数量重复相加。
- 无效税额在发送保存请求前被拒绝。
- 后端国家与物流商税费规则分别校验。
- 财务保存接口验证权限、CSRF、过期版本和并发版本冲突。

另归档本轮燕文运价及历史报价修正结论。所有被选择的文件均为测试或文档；不改变生产业务代码、配置、数据库迁移或依赖。

## 保留在原工作区的内容

| 工作区或类别 | 判断与处理 |
| --- | --- |
| 主目录旧业务修改 | 已发布或已由更完整实现替代。保留原文件，不用旧代码覆盖当前生产。 |
| 主目录 V40/V41/V42 SQL 草稿 | 正式版本中相应功能已用 V43/V44/V49 发布；原编号已被其他迁移占用，不能重复提交。 |
| overview-performance-20260921 | 正式版已有 `/purchase-products/analytics-catalog` 与完整性校验；旧草稿的另一套采购概览接口不再合入。 |
| quotation-logistics-import-reliability | 包含移除覆盖区域审核、改变未适配计费渠道发布行为的旧草稿，与正式保护逻辑冲突，不能视为遗漏修复直接发布。 |
| quotation-weight-range-only | 发布反馈草稿已由包含进度和轮询的正式版本替代。 |
| quotation-yanwen-zhengzhou | 相关修改已在正式提交历史中。 |
| quotation-cancel-withdraw-20260924 | 取消/撤回功能由另一进行中的任务维护，本次不取用未完成代码。 |
| 旧压力测试、真实语料测试、私有验收文档 | 与特定历史快照和运行环境绑定；不打包进生产，也不提交原始商业数据或凭据。 |

主目录及旧工作区仍可能显示未提交状态。本次没有重置、删除或暂存他人工作，也没有为了让状态清零而提交旧文件。

## 部署判断

生产所需业务改动已经包含在上述生产基线；燕文数据修正也已执行并回读验证。本次测试/文档提交不需要重建或切换生产容器。生产版本、Git 提交和本次测试结果分别报告，避免将“代码已提交”等同于“已重新部署”。
