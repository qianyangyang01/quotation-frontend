# 报价单新增数量列与回到顶部：本地验收

日期：2026-09-12。工作区：quotation-customer-quote-sheet，基线 4cef999。

## 实现边界

客户报价单展示、图片与 TSV 同用动态数量列和本页字段编辑。当前报价复用 quantityCostBreakdown 计算新增数量；记录只复用保存的数量价格，其他数量明确手填。未新增 API、数据库、图片上传、持久化或下载。原三个矩阵的 Excel 复制、正式报价保存与计算实现未修改。

## 自动化覆盖

- 1–10 价格列；8/10/20/50 等自定义数量、组合 set/sets、两位小数、0 与空价格。
- 空数量、0、负数、小数、重复、超安全整数、11 列、计算异常、非法手填金额与公式注入保护。
- 模型／组件／TSV／PNG 使用同一顺序和值；同国同物流商多个渠道保留，区域与渠道身份不因展示编辑变化。
- 首次手填金额的 Vue 响应式同步、快速改数量时丢弃旧 PNG、删除再添加不串用手填价、行重排保持编辑。
- 源价格重算与商品切换清除旧价格；报价条件变化保留所选数量；历史 Custom 数量缺失时保留原价，不猜数量。
- 调用实际 QuotationSystemView.quantityCostBreakdown，单品/组合 × 国家范围费用配置，逐个核对 1–10 数量的税费及附加费金额，保证单次固定费用没有倍乘。
- 拖拽与键盘排序同时移动数量及手填价格；PNG 和 TSV 导出顺序一致，键盘首尾边界与焦点保持通过。
- PNG 几何 1–10 列逐列检查；回到顶部在页面与记录对话框中正确识别滚动容器，支持减少动态效果。
- 原有剪贴板权限拒绝、未就绪报价、渲染取消、商品上下文、记录复制回归继续通过。

## 本地真实浏览器验收

使用 Codex 内置浏览器挂载真实 Vue 编辑器、Canvas 渲染器及剪贴板代码；六行/五十行是明确标注的本地虚构数据，未连接业务接口。

1. 右侧添加列并填 8，立即带出对应模拟计算金额；新增到十列后按钮禁用。
2. 十列六行 PNG 实测 2400×989，表头、所有价格列、完整四条说明均显示；增加数量列扩大画布，不缩小绘制字体。
3. 点击复制图片，通过真实 Ctrl+V 在本地粘贴区获得 PNG，实测与预览一致为 2400×989。没有向客户发送消息；没有将此验证描述为微信/Excel 客户端验收。
4. 点击复制报价数据后 Ctrl+V：得到 7 行（含表头），每行严格 15 列，末列表头 10 pcs (USD)，第一行末价 $31.00，与模拟报价一致。
5. 五十行十列分为三张：24+24+2；第一张 2400×1729。仅最后一张包含说明，翻页不遗漏渠道。
6. 滚到页面约 3558 像素处显示回到顶部，点击后 scrollY 为 0。
7. 真实鼠标将第 3 个价格列拖到第 1 列，顺序变为 3/1/2/5，首行价格对应变为 13.50/7.15/10.05/19.05；键盘左右键移动正常。
8. 本地工作区内无图片业务写入、无业务数据保存调用；静态 Logo 仍为原素材。

## 交付状态

本地实现。类型检查、修改文件 ESLint、生产构建通过。最终全量 Vitest：88 个文件通过，546 项通过，1 项既有跳过；耗时 10.19 秒。新增及扩展测试覆盖本次动态列、排序、历史兼容和滚动行为。没有提交 GitHub 或部署本轮变更，生产基线仍与本轮开发分开。

本地交互入口：http://127.0.0.1:5174/.codex-tmp/customer-quote-sheet-inline-columns.html
