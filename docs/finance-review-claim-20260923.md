# 财务审核领取占用

## 范围与基线

本地实现基于 `10a8eca6151abd80c95509d0a4f2b6c865b7204c`（本地发布证据对应 quotation-2026.09.23-15），独立分支 `codex/finance-review-claim-20260923`。桌面主目录及其他任务工作区未被覆盖。本任务未部署、未操作生产业务数据。

最新基线已有财务待审／可报价／价格有误三个状态、审核历史和员工端轮询。本次在此基础上增加占用，保留既有结论及原报价确认、成交逻辑。

## 使用方式

- 列表显示待审、审核中（人名和开始时间）、审核结论；新增审核中和我正在审核筛选。
- 点击开始审核才占用，返回最新报价详情；普通查看不占用。
- 领取人可审核完成（可报价或价格有误），或取消审核。价格有误需要说明原因。
- 其他财务只能查看。超级管理员可填写原因解除他人占用，不能替他直接提交。
- 已完成的记录可显式重新审核，形成新的占用和审核记录。
- 不设置超时释放：关闭页面、断网、退出账号后保留占用；本人再次登录继续，无法继续时由超级管理员解除。
- 失去占用后旧页面、重复点击、旧版直接修改审核状态接口不能绕过校验。

## 数据和并发

新增 `quotation_review` 表（迁移暂定 **V50**），按报价 ID 唯一关联，记录状态、领取账号、独立审核版本以及审核人/时间/备注/历史。没有审核元数据的旧记录按待审读取；迁移只复制明确保存的 approved/rejected 结论，不推断成交或报价确认等于财务审核。

所有审核写入与原报价编辑统一先锁 `quotation_record` 行，再访问审核表。数据库事务串行检查领取资格；两个版本分别控制报价内容和审核占用。开始、完成需要报价内容版本匹配；取消、解除也必须匹配原审核占用版本。没有跨 HTTP 请求持续持有数据库锁。

领取、取消、完成、解除不会修改原报价 payload、业务 version、updatedAt、金额、成交结果或创建时间。审核状态由列表、详情、状态轮询统一覆盖旧 payload 字段；分页筛选在数据库执行，批量读取当前页审核数据，避免逐条查询。

## 联动规则

| 模块 | 行为 |
| --- | --- |
| 客户报价及数量列 | 实际报价单单元格变化后，审核中保留领取人但拒绝旧内容完成；已完成审核变回待审，留存原结果历史 |
| 客户名称 | 修改后同样需重新核对，避免审核对应错客户 |
| 普通备注、报价确认、成交状态 | 不重置财务审核；在审页面遇到报价业务版本变化仍要求重新载入最新详情 |
| 编辑器和轮询 | 轮询只更新状态，不悄悄更新正在核对的报价内容版本；原占用失效后不自动换用新占用版本提交 |
| 客户报价图片、复制、原CSV | 保持原字段和金额，内部审核信息不注入对客报价单；筛选导出与列表范围一致 |
| 总览统计 | 原金额、数量、转化率和业务待处理口径保持原样，审核不会更新“最后修改”时间 |
| 再次发起 | 既有显式输入白名单不复制审核；服务端创建重置包括领取账号在内的全部审核字段 |
| 采购／物流／财务设置 | 不改计算和数据；历史快照不随当前参数变化重算或批量重置审核 |
| 员工权限 | 只读取自己报价的审核及历史，不获得审核权限；普通财务无管理员解除能力 |
| 培训系统 | 无代码、数据、配置或服务变更 |

## 并行任务协调及后续合并

- 报价页布局任务位于 `quotation-compact-layout-20260923`，本次不改 QuotationSystemView 或其报价布局组件。
- 采购修改记录任务计划先发布，约定占用 **V49**。本次 V50 尚未发布；未来发布必须合入最新生产提交并再次核对迁移顺序和校验和，不能开启 out-of-order 或 repair 绕过。
- 全量迁移回归中，从 V23 升级的迁移数在单功能分支为 26，合并采购 V49 后应为 27，再合并生命周期 V51 后应为 28；即使 Git 自动合并同一计数修改，也必须重新执行迁移测试。
- `LogisticsAcceptanceMigrationTest` 的最新迁移版本断言同步更新为 50；合并采购任务时保留其采购索引断言，合并生命周期任务后更新至 51。
- 报价归档／回收站任务在独立分支 `codex/quotation-record-lifecycle-20260923`，约定 V51。双方共同涉及 QuotationController、QuotationRecordQuery、QuotationRecordsView、quotationRecords 等文件，必须三方合并，禁止覆盖文件。
- 归档任务交付的本地提交为 `ddef21cd90573aa726aa8809751081c99d3954a5`，本分支未合入其功能，联合合并与回归属于上线前待办。
- 为生命周期联动提供 `QuotationReviewService.currentStatus(QuotationRecordEntity)` 权威查询。归档保护应使用它，而非旧 payload 审核状态，审核中报价不能归档或移入回收站。
- 合并生命周期功能时，在审核接口取得 `records.lockById(id)` 后、`reviews.change(...)` 前加入 `QuotationLifecycleController.assertActive(row)`；报价编辑入口同样检查。恢复记录不应恢复旧占用。当前基线尚无生命周期模块，不引入对未完成代码的依赖。

## 发布及回退注意事项

需要前后端一起更新。旧页面提交旧格式会明确提示刷新，不能继续直接设为已审核。正常关闭页面不是释放动作。

上线前备份，执行新表迁移并校验原报价行数及业务指纹不变；用四个财务账号验证同单争抢、释放及改价联动。生产发布与登录验收尚未执行。

新表是审核状态的权威来源。上线产生新审核记录后，不可直接退回只读 payload 审核字段的老后端，否则会显示旧结论并失去占用保护；应保留本版本审核接口做前向修复，或另行制定审核状态兼容回退，不删除新表或审核历史。

## 验证

2026-09-23 本地验证：

- 前端完整 Vitest：167 个测试文件通过、9 个跳过；1139 项通过、11 项跳过。TypeScript 检查、涉及文件 ESLint、Vite 生产构建通过。
- 后端报价模块及 Flyway 相关回归共 66 个场景：首次 65 项通过，1 项发现旧测试记录 JSON 编号与数据库主键不一致。统一使用数据库主键后，复测该失败场景及审核、查询控制器、迁移相关共 21 项全部通过。没有将本结果等同于全后端或 GitHub CI 全绿。
- 审核集成用例分别在 H2 和真实 PostgreSQL 上执行，包含四个独立财务账号同时争抢、改价与完成审核并发、越权、管理员解除、重复/过期提交、审核历史及成交联动；PostgreSQL 测试未跳过。旧数据迁移校验报价业务数据不变。
- 额外执行 `LogisticsAcceptanceMigrationTest` 全类 8 项全部通过，确认 V50 加入完整迁移链后原物流验收及业务数据保护回归正常。
- 浏览器使用本地模拟记录核对列表及详情：开始审核、本人完成、他人只读、审核人展示正常。临时页面明确标注模拟环境；本结果不是生产登录验收。
- 未推送 GitHub、未发布、未修改生产数据。后续发布前需合入届时最新生产分支，并回归采购 V49、审核 V50、生命周期 V51 的联合迁移及交互。
