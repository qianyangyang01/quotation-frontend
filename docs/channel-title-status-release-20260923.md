# 渠道识别与处理状态筛选位置

基于 `433828079080754fefea993b98dd7a66be963e1c`（quotation-2026.09.23-22）独立整理，保留该版本的审核结果、审核状态筛选、只看我的及底部审核按钮。

- 已有渠道更新优先采用表内产品代码、明确渠道列和标题证据；只有缺少表内身份时才使用 Sheet 名。标准模板的空渠道名及通邮横向分区价格表也使用相同识别规则。
- 标题支持登记名称/别名、明确标签、报价表/价格表后缀和产品代码；不进行模糊猜测。保留标题单元格证据、原始 Sheet/行号及价格读取规则。
- 明确列值不被标题覆盖；标题与代码冲突、多个登记渠道、停用或跨物流商渠道继续拦截。解析版本升级为 `sheet-titles-2026.09.23-v2`，防止复用旧解析检查点。
- 将原“处理状态”下拉框移至记录表的“处理状态”列头；保留 v-model、选项值和无障碍名称，审核筛选及其他筛选行为不变。
- 不含数据库迁移、历史价格或业务记录写入。

## 本地验证

- 物流解析和相关回归：125 项，106 通过、19 项可选真实文件用例跳过，零失败。
- 报价记录分页、图片、生命周期、财务审核：26 项通过。
- 页面 ESLint 和 TypeScript 检查通过。
- 发布须等待同主机其他发布完成，以实际线上版本和两把发布锁作切换前置条件；完整 CI、镜像资源校验、备份及线上验收证据保存在本地版本发布包。
