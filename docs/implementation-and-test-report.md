# 报价系统生产故障整改实施总览

> 历史报告：其中记录的数据迁移上传、审批、执行、回滚及浏览器导出能力已于2026-09-04由Flyway V34退役。当前系统不再提供相关页面或API，`business_migration_batch`表将在应用V34时删除；已形成的正式业务记录和审计日志不受影响。

- 报告日期：2026-08-23
- 分支：`codex/quotation-production-remediation`
- 基线：`d6a07a8a8dcf7f3a8ba7f04533121a3734f9b2af`
- 工作树：`C:\Users\25490\Desktop\quotation-production-backend`
- UAT版本：`quotation-uat-remediation-20260823-06`
- 候选生产版本：`quotation-2026.08.23-01`

## 1. 已完成实现

- Spring Boot 4统一Jackson 3，修复JSON写接口500。
- 采购Excel服务端解析、公式及人民币金额兼容、阻断确认、图片暂存发布、分页和SKU冲突校验。
- 增加`pending_template`、`ready`、`disabled`目录状态；测试SKU必须更换为完整业务SKU并通过乐观锁才能转正式。
- 物流商、渠道、版本、规则、导入、发布、回滚数据库化；速猫66文件、3,298有效价格段及81异常隔离可审计迁移。
- 供应商、财务、报价、PDF、哈希分享令牌、作废恢复和审计闭环；客户信息作为报价快照保存。
- 增加业务迁移上传、审批、执行、差异、错误、幂等和批次回滚。
- 增加`GET /api/v1/quotation-readiness`；正式报价必须同时满足正式采购、已发布物流和五类完整财务设置。
- 浏览器只读历史数据导出默认排除账号、密码、Cookie、Session、草稿、演示及培训数据。
- Flyway V6–V12、独立UAT Compose、JaCoCo硬门禁与服务器Testcontainers。

## 2. 测试结论

- 前端：ESLint、TypeScript、生产构建通过；6文件、13项Vitest通过。
- 后端本地：87项测试、0失败；2项Docker集成因本机没有Docker跳过。
- 后端服务器：87项测试、0失败、0错误、0跳过；Flyway V1–V11通过。
- 覆盖率：行79.30%、分支61.18%，达到70%/60%门禁。
- UAT：5个报价容器healthy；培训7容器和生产报价5容器未变化。
- 数据：30条待补全SKU、12个物流商、66个渠道/草稿、3,298有效价格段、81隔离错误、2条历史报价全部验证。
- 正式业务就绪数：采购0、已发布物流0、有效财务配置0；报价创建按设计返回422。

## 3. 发布边界

- 30条`TESTP*`仅作模板待补全目录，永久禁止直接参与报价。
- 66个速猫版本均为草稿；5个含异常版本禁止发布，其余也不自动发布。
- 财务表的5条空占位不算有效配置，不生成或复制模拟财务数据。
- 两条历史报价映射至生产独立账号`QIANYANGYANG`，不接入培训用户体系。
- 生产只允许操作`quotation-*`容器、网络、卷、桶、目录和备份；培训系统仅做只读回归。

## 4. 尚未执行

- Git拆分提交、推送、远端SHA与GitHub质量门禁。
- 生产报价数据库和MinIO发布前完整备份。
- `quotation-2026.08.23-01`生产部署、认证迁移、幂等复核与维护态解除。
- 生产TLS、Cookie、安全头、公网8088、双向Session拒绝及培训回归验收。
- 真实10万级图片分卷迁移仍属于后续业务数据搬迁，不在本次30条模板数据发布范围内。

历史UAT证据见`docs/uat-acceptance-report.md`。
