# 顺丰折扣规则与草稿复验修复

状态：本地实现与定向验证完成。用户已授权推送与部署，实际发布结果以发布包的版本清单和验收记录为准。正式运价不自动审核发布。

基线：当前生产提交 `03769e0669115a6919e642d9a24e45cf5cbbf313`，独立分支 `codex/sf-blank-discount-20260916`。

## 用户确认的规则

1. 行内有有效折后运费时，直接采用折后运费，不要求折扣率，也不再乘折扣。
2. 折后运费为空、折扣率有效时，采用原运费乘折扣。
3. 折扣率与折后运费都为空时，采用原运费。
4. 已填写但无效的折后运费、无法计算的公式、零价、无有效折后运费的无效折扣继续阻断。

补充识别顺丰表头“折扣后运费”。空白判断采用真实单元格类型，公式返回空字符串不当作空白。记录行级计价依据。

## 已有草稿

顺丰服装专线 V3，`df169a65-c32e-473d-9fdd-85d040838c7b`，43 条价格。西班牙第 21、22 行的 72、76 已保存，但剩余 6 个顺丰解析错误和两个旧阻断原因没有随价格修正消除。

修复后的重新校验会读取保留的原表，确认这两列确实为空，仅移除对应行的已解决错误和旧计费阻断，保留价格、每票费、最低计重、重量段、原始证据及其他问题。修正规则写入操作历史，保留指纹并发保护，并同步批次摘要。前端明确显示问题数量与涉及价格条数。

服务器原文件已标记 deleted。桌面原表与服务器记录的 SHA-256 一致：

`7efb25e9a968826c44630f229e028b6c09ae8d746110e7be4920b27ad6972cdf`

后续线上应用需要在授权发布时，先备份并恢复这份相同文件及其可读状态，再调用现有草稿重新校验接口。不要创建替代草稿、重新导入覆盖或直接清空错误。原表不可读时后端继续阻断并提示原因。

## 验证结果

- 后端定向回归：55 项，51 通过，4 项按条件跳过；真实 PostgreSQL 草稿保存、批次同步、审计、指纹拒绝测试执行通过。
- 用户补充的敏货规则：新增英国折扣率为 `-` 或空、结算价为 33/38/40 的 XLS 与 XLSX 案例，执行通过。
- 修复原文件 deletedAt 为 null 的判定后，重新执行草稿修正相关 7 项，全部通过。
- 前端定向回归：18 项通过；Vue/TypeScript 类型检查通过；`git diff --check` 通过。
- 使用真实原表及只读生产快照复验：现有草稿 6 个错误降至 0，43 条价格的价格、每票费、最低计重、重量上下限、原始行证据均保持一致。
- 原表重新解析：服装西班牙为 72/76 元/kg，每票 18 元；敏货英国为 33/38/40 元/kg，每票 16 元，计价依据为 settlement。
- 原始文件分析前后哈希一致。

证据目录：`C:\Users\25490\Documents\ProjectWorkspaces\quotation\outputs\sf-blank-discount-20260916`，包含源文件哈希核对、只读草稿快照、复验结果和真实原表解析结果。

## 独立待核查问题

服装草稿最低计重 0.5 kg 与 11 个档位上限冲突，修复后仍被计费验收阻断，不能直接发布。

敏货法国原表两组重量区间不一致：第 17 行原价区 I17 为 0.001–0.4 kg，折后价区 M17 为 0.001–0.2 kg；第 18、19 行 M 列均为 0.201–0.4 kg，而 I19 为 0.401–30 kg。当前解析仍产生两个重量重叠/重复错误。此次未猜测或修正这些边界，也未放行该渠道。
