# 当日验收收尾

基于已发布的 quotation-2026.09.22-06 / 8def489，保留普通客户显示改名和此前各任务变更。

上方“搜索全部国家或渠道”过滤渠道后，列表已按关键词收窄，但顶部和分页计数只判断下方渠道搜索框，导致巧捷实际匹配3条却显示共33条。两处现在共用计数字段，任一搜索框有有效关键词时显示“匹配3/33条渠道”；清空搜索恢复总数，分页继续按过滤结果计算。未改变授权、计价、选择和保存逻辑。

本地70项现有回归通过，覆盖搜索、再次发起、草稿保护、保存按钮状态与实际保存请求、数量预检及等级配置；类型检查、构建和文件ESLint检查通过。线上浏览器验收在对应发布包中追加。

灰色按钮原反馈未包含按钮旁的拦截提示，无法追溯当时原因。现行组件的拦截信息、重试和保存中状态回归通过，上一轮已读验收发现业务员成功保存的组合报价QT202609220344332DD1C2，图片及复制正常。此项按“当前样本正常，原始原因未能追溯”记录，不把不明原因归为某个已修复故障。

发布仅更新前端，承接-06；后端及parser继续保留-03原镜像，避免触发已发现的历史Flyway换行校验差异。不修改生产草稿、历史报价、财务配置或物流价格来制造验收结果。
