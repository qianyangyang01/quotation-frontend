# 多角色同步与报价保存回归（2026-09-17）

基线：生产 `quotation-2026.09.17-09`，提交 `2455f668342b3350b92946c2bb8440f30e32937a`。在独立工作区与仅监听 `127.0.0.1:18117` 的 Docker 项目 `quotation-roles-deep-20260917` 验证；使用当日数据库备份，覆盖最新七类财务设置，禁用复制的生产账号，创建 50 个独立测试账号。生产未执行压测或创建测试报价。

## 已复现与修复

- 后到的旧财务 GET 覆盖已确认 PUT；后到的旧 PUT 覆盖较新的 GET。缓存按每类设置版本保留最新值；账号缓存清理后丢弃旧请求的缓存写入。
- 页面校验与提交之间管理员修改系数，基线仍接受旧价格。新客户端提交实际计价所用的七类 `financeVersions`；后台事务检查全部版本并持有共享行锁至保存结束。新版本陈旧请求返回 409，要求更新报价；成功请求重放仍返回原记录。旧客户端缺失字段沿用原有校验，历史记录不回算。
- 保存按钮受阻时，底部仍显示“可以保存”。改为显示同一个真实阻断原因，并提供加载/同步错误的原页重试。
- 临时图片重置键此前含重载过程中的中间商品状态。改为按账号、模式与实际 SKU 选择重置，并在商品恢复完整后再替换页面商品；同组商品重新计价保留图片，换账号、换商品或离开页面仍清除，不新增持久化。

## 验证范围

- 2 管理员、2 财务、36 员工、5 采购、5 物流独立 Cookie 会话。
- 七类财务设置值/版本一致；六种已配置物流属性的英国规则跨角色一致；报价账号已发布物流清单一致。
- 每类财务设置 12 个同版本并发写入仅 1 个成功；全部 50 账号随后读取相同结果。
- 40 个报价账号草稿隔离；同草稿/采购记录 50 次竞争写入仅 1 个成功；其余 409。
- 员工不能修改财务、采购或访问物流管理；采购/物流不能访问报价；角色撤销、恢复立即应用于既有会话；禁用账号返回 401。
- 50 次相同幂等键保存只产生一张报价；其他员工不能读取或修改该报价；非法佣金阈值被拒绝。
- 各财务配置在预检后修改：旧版本接受，新版本拒绝。客户系数实际变更同样复现并通过修复验证。
- 用户反馈组合 `BK2601914 + TC2602030`、三个美国渠道，在隔离库的基线页面保存并读回成功，1/2/3/5 套价格与截图一致。尚未捕获用户原 Chrome 页面当时的内存状态；已确认并修复禁用原因提示矛盾，不把该截图直接归因为某个具体网络错误。

## 证据与边界

前端完整回归 855 通过、2 条指定语料测试跳过；后端 542 项中 513 通过、29 条外部语料/条件用例跳过。新增 PostgreSQL 财务版本与事务锁测试实际执行 2/2 通过，未跳过。ESLint、Vue 类型检查、Compose 隔离检查及 Docker 构建通过后方可发布。

候选接口验收 75/75 通过。持续负载：15 秒预热、300 秒测量、50 独立账号、最高 50 个同时在途 HTTP 请求，共 39,872 请求，非预期失败 0；预期越权 403 单独按权限断言核对。期间 155 次财务修改、48,146 次设置版本/值核对、5,276 次草稿隔离核对。财务 GET P95/P99 511/930ms，物流规则 P95/P99 968/1397ms，草稿混合读写 P95/P99 384/608ms。后端与数据库各限 2 CPU，报告包含资源采样；不含公网往返与渲染。

完整脚本、基线与候选结果、负载资源采样、构建日志保存在本机 `Documents/ProjectWorkspaces/quotation/outputs/roles-sync-deep-20260917`。该目录包含受限业务备份，不提交仓库。

接口并发测试、浏览器验收、生产发布分别记录。角色权限故意不同，私有草稿和记录不能要求跨账号相同。隔离环境响应时间不代表公网或浏览器渲染耗时，也不构成对任意并发规模的保证。
