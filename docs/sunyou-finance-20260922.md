# 顺友三个渠道的财务配置

2026-09-23 用户确认美国、英国、加拿大、澳大利亚全境不走这三个渠道；“奥”已明确为澳大利亚。本次为有版本保护、可审计的数据配置发布，应用版本不变。

## 原表及范围

- `物流渠道属性表.xlsx`：Sheet1 第108–110行，9列属性，共27格。
- `关税(2)(1)(1).xlsx`：Sheet1 第93–95行，15列费用及限制，共45格。
- 顺邮宝挂号：普货、化妆品、服装、带电。
- 顺速宝(特货)：化妆品、带电。
- 顺速宝Plus：化妆品、带电、纯电、香水。
- 渠道身份同时核对公司UUID、正式渠道UUID、渠道代码、产品代码SYBRAM/SSBRAM/SYEPL及物流商。不能仅按显示名称批量覆盖。

欧盟金额沿用既有导入规则：Excel缓存公式结果以USD解释，按分计费为3.51美元/单，固定费只收一次，不改系统汇率。原公式、缓存值和文件SHA256保留在审计证据中。
美国、英国、加拿大、澳大利亚、新西兰、墨西哥不可发；罗马尼亚额外处理费项为不可发，沿用欧盟加本国处理费逻辑阻断。阿联酋、沙特、哥伦比亚、约旦、摩洛哥、阿曼无税；卡塔尔无处理费。共追加45项明确规则，不改变既有规则及国家全局启停。

原表偏远三列分别留存作为来源证据；用户明确英国、加拿大全境不发，因此无需改变英国附加费设置。禁止的七个国家同时不授予目标渠道属性权限。

属性覆盖三个渠道允许服务的已发布目的地。财务原有国家启停不变；未勾选的属性不授权。按项目现有国家别名表验证匹配，原有无法匹配的旧国名不作无效新增授权，使用正式运价的国家名称；不扩大全局别名表影响其他渠道。

## 验证及发布

准备脚本只读取原始工作簿和生产只读快照，输出在本地发布目录。原始Excel和完整商业数据不提交Git。
前端真实计算函数核验2952次目标税费、1172条属性授权、189次禁发授权拦截、1300次属性与运费兼容性；27060次其他渠道费用对比完全一致。Java原生校验验证同一载荷及全部税费快照；精确SHA256和接受/拒绝数量留存在发布目录的native-tax-evidence.json中。

配置属于数据发布，现有应用已支持，不需要重建或重启应用。最终事务前仍需刷新基线、重跑精确载荷校验、备份。事务锁定财务设置并核对所有版本及载荷、活动物流库、正式价格/渠道指纹和公司身份；任一变化即回滚。只更新明确选定的财务键并写审计，不改物流价格、历史报价或培训系统。
SQL生成器强制核对已确认的七个禁发国家、前后端验证通过及载荷SHA256，只允许更新tax-settings和channel-policies。发布后重新读取配置比对，确认其他设置和渠道价格不变，再做只读页面验收。私有基线、备份路径及发布结果存放在工作区之外的sunyou-finance-20260922发布目录；不提交商业数据。
